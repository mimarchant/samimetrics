<section class="contact-form-wrapper py-5 px-4">
  <form action="" id="my-form">
    <h4 class="text-center text-xl lg:text-2xl text-white">
      SAMILIZATE CON UNO DE NUESTROS EJECUTIVOS
    </h4>

    <div
      class="flex flex-col lg:flex-row items-center justify-center mt-10 gap-x-10"
    >
      <span>
        <img src="sami-pirata.webp" alt="" />
      </span>

      <div class="flex flex-col lg:flex-row gap-x-4 mt-5 lg:mt-0">
        <span class="flex flex-col">
          <span class="text-white sami-info tracking-wider mb-4 lg:mb-0">
            sami@samimetrics.com
          </span>
          <span
            class="text-white sami-info lg:hidden tracking-wider mb-4 lg:mb-0"
          >
            skype: Samimetrics
          </span>
          <span
            class="text-white sami-info lg:hidden tracking-wider mb-4 lg:mb-0"
          >
            +56 2 3230 0966
          </span>
          <input
            type="text"
            name="name"
            placeholder="Nombre"
            class="mt-4 p-1 rounded-md"
          />
        </span>

        <span class="flex flex-col">
          <span class="text-white sami-info hidden lg:block tracking-wider">
            skype: Samimetrics
          </span>
          <input
            type="text"
            name="email"
            placeholder="Introducir Email"
            class="mt-4 p-1 rounded-md"
          />
        </span>

        <span class="flex flex-col">
          <span class="text-white sami-info hidden lg:block tracking-wider">
            +56 2 3230 0966
          </span>
          <input
            type="text"
            name="phone"
            placeholder="Introducir Teléfono"
            class="mt-4 p-1 rounded-md"
          />
        </span>
      </div>
    </div>
    <div class="flex justify-center lg:justify-end">
      <button
        type="submit"
        class="px-4 py-2 rounded-md mt-4 lg:mt-0 mr-5 lg:mr-10"
      >
        Enviar
      </button>
    </div>
  </form>
</section>

<style>
  .contact-form-wrapper {
    width: 100%;
    background: -webkit-linear-gradient(left, #0071bc, #49c9e6 100%);
    background: linear-gradient(to right, #0071bc, #49c9e6 100%);
  }

  .sami-info::after {
    content: "\0020";
    display: block;
    width: 100%;
    height: 1px;
    background-color: lightgray;
  }

  button {
    background: -webkit-linear-gradient(left, #005f99, #33b3d6 100%);
    background: linear-gradient(to right, #005f99, #33b3d6 100%);
    color: white;
  }

  input::placeholder {
    font-size: 12px;
  }
</style>

<script>
  document

    .getElementById("my-form")
    .addEventListener("submit", function (event) {
      event.preventDefault();
      const formData = new FormData(event.target);

      const data = Object.fromEntries(formData.entries());

      alert("Correo enviado: " + JSON.stringify(data));
    });
</script>
